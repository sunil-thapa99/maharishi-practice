#include <iostream>

using namespace std;

int isCentered5(int a[ ], int len){
    
}

int main(){
    int a[] = {3, 2, 10, 4, 1, 6, 9} ;
    int len = sizeof(a)/sizeof(a[0]);

    cout << endl;
    return 0;
}